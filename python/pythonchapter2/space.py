name = "sarthak is good   boy and "
print(name.find(" "))
# replace it with single space
print(name.replace("  "," "))
# string are immutable which mean you cannot change them by running function on them  
print(name)